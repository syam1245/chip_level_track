{
    "name": "chip-level-track",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "npm start --prefix server",
        "build": "npm install --production=false --prefix client && npm run build --prefix client && npm install --prefix server",
        "dev": "concurrently \"npm run dev --prefix client\" \"cd server && node server.js\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "node-fetch": "^3.3.2"
    }
}
